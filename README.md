# KnowMe — платформа обмена знаниями между специалистами

## 📌 О проекте

**KnowMe** — это платформа для обмена опытом между специалистами разных направлений.  
Пользователи могут находить менторов по категориям и фильтрам, изучать их профили и связываться для обмена знаниями или услугами (в том числе на финансовой основе).

Проект сфокусирован на фронтенд-части и демонстрирует подход к построению масштабируемого, SEO-дружественного SSR-приложения с чистой архитектурой и строгой типизацией.

---

## 🧭 Основной пользовательский сценарий

1. Пользователь регистрируется или авторизуется
2. Ищет менторов по категориям и фильтрам
3. Просматривает превью популярных менторов в слайдере
4. Переходит на страницу конкретного ментора и изучает подробную информацию
5. Связывается с ментором через чат (частично реализован, пока на моках)
6. Договаривается об условиях обмена знаниями или услугами

---

## 🎯 Цели проекта

Проект создавался с фокусом на:

- разработку **SSR-приложения** с учетом SEO
- прозрачную и поддерживаемую архитектуру
- четкое разделение бизнес-логики и UI-слоя
- строгую типизацию (TypeScript)
- масштабируемость без избыточной сложности
- использование готовой UI-библиотеки (MUI) без визуального перегруза

---

## 🧱 Архитектура

Архитектура проекта ориентирована на бизнес-домены и реальные пользовательские сценарии.

**Ключевой принцип:**

> Feature-oriented архитектура с четким разделением бизнес-логики и UI-слоя, без избыточных абстракций.

### Основные слои

- `pages/` — маршруты Next.js и SSR
- `components/` — бизнес-компоненты (по фичам)
- `ui-components/` — переиспользуемые UI-элементы без бизнес-логики
- `api/` — работа с API, axios-инстанс, трансформеры данных
- `providers/` — контексты и глобальные провайдеры
- `types/` — DTO и общие типы
- `utils/` — вспомогательная логика
- `constants/` — мок-данные и справочники

Архитектура осознанно **не доведена до FSD**, так как для текущего масштаба проекта это привело бы к избыточной сложности без практической выгоды.

---

## 🔌 Работа с данными

- Все данные на текущий момент — **моки**
- Инфраструктура для реального API уже заложена:
  - `axios` instance
  - `react-query` (queries и mutations)
  - подготовка к переходу на `Next.js API Routes`

Это позволяет без изменения архитектуры подключить реальный backend.

---

## 🛠️ Используемые технологии

- **Next.js** — SSR, SEO, routing
- **React + TypeScript** — типобезопасность и поддерживаемость
- **React Query** — работа с серверным состоянием
- **MUI + Emotion** — базовая UI-система
- **React Hook Form + Yup** — формы и валидация
- **Axios** — HTTP-клиент
- **ESLint (Airbnb) + Prettier** — качество кода

---

## 🚧 Что в разработке

- полноценный чат
- звонки и работа с потоками данных
- реальный backend
- Next.js API Routes

---

## 🔮 Возможные улучшения

- интеграция реального API и авторизации
- расширение чата и real-time взаимодействий
- покрытие ключевых бизнес-фич unit-тестами
- оптимизация кеширования и SSR-стратегий

---

## 🚀 Запуск проекта

### Требования

- Node.js >= 20.9.0

### Установка и запуск

```bash
npm install
npm run dev
```

### Дополнительные команды

```bash
npm run build
npm run start
npm run lint
npm run check-types
```

---

## 📎 Статус проекта

Проект находится в активной разработке и используется как демонстрация архитектурного и инженерного подхода к созданию frontend-приложений.
