import { IconButton, FormGroup, styled } from '@mui/material';

export const StyledIconButton = styled(IconButton)({
  padding: 0,
});

export const StyledForm = styled(FormGroup)({
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'center',
  width: '400px',
});
